{% extends 'layouts/base.html.twig' %}

{% block page_title %}{{ 'Clients'|trans }}{% endblock %}

{% block page_title_actions %}
    <a href="{{ path('app_client_new') }}" class="btn btn-primary d-none d-sm-inline-flex">
        {{ ux_icon('tabler:user-plus', {class: 'icon'}) }}
        {{ 'Create new client'|trans }}
    </a>
    <a href="{{ path('app_client_new') }}" class="btn btn-primary btn-icon d-sm-none" aria-label="{{ 'Create new client'|trans }}">
        {{ ux_icon('tabler:user-plus', {class: 'icon'}) }}
    </a>
{% endblock %}

{% block content %}
    <twig:ClientList />

    {{ include('components/_manual_entry_modal.html.twig') }}
{% endblock %}
