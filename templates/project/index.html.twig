{% extends 'layouts/base.html.twig' %}

{% block page_title %}{{ 'Projects'|trans }}{% endblock %}

{% block page_title_actions %}
    <a href="{{ path('app_project_new') }}" class="btn btn-primary d-none d-sm-inline-flex">
        {{ ux_icon('tabler:text-plus', {class: 'icon'}) }}
        {{ 'Create new project'|trans }}
    </a>
    <a href="{{ path('app_project_new') }}" class="btn btn-primary btn-icon d-sm-none" aria-label="{{ 'Create new project'|trans }}">
        {{ ux_icon('tabler:plus', {class: 'icon'}) }}
    </a>
{% endblock %}

{% block content %}
    <twig:ProjectList />
{% endblock %}
